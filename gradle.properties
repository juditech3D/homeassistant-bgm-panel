org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Le compilateur Kotlin tourne dans son propre démon, avec sa propre JVM : sans cette
# ligne il relit les sources avec l'encodage par défaut de Windows (cp1252) et les
# caractères accentués, le point médian et le symbole degré ressortent en « Â· », « Â° ».
kotlin.daemon.jvmargs=-Xmx1536m -Dfile.encoding=UTF-8

android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
